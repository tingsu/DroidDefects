<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:orientation="vertical"
  android:layout_width="fill_parent"
  android:layout_height="fill_parent" android:id="@+id/mainFavoritesEdit">
    <ScrollView android:layout_width="fill_parent" 
    android:layout_height="fill_parent" android:id="@+id/scrollView1" 
     android:layout_marginBottom="50dip">
        <TableLayout android:id="@+id/tblFavoritesEdit" 
        android:layout_height="wrap_content"
         android:layout_width="fill_parent"></TableLayout>
    </ScrollView>
    
    <RelativeLayout 
        android:layout_width="fill_parent"
        android:layout_height="50dip"
        android:layout_alignParentBottom="true">
	    <Button android:layout_width="wrap_content" 
	    android:layout_height="wrap_content"
	    android:layout_alignParentBottom="true"
            android:layout_alignParentLeft="true"
	    android:text="@string/EditFavAdd" android:id="@+id/btnEditFavAdd"></Button>
	    	    <Button android:layout_width="wrap_content" 
	    android:id="@+id/btnEditFavRemove" android:layout_height="wrap_content"
	    android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/btnEditFavAdd"
	    android:text="@string/EditFavRem"></Button>
    </RelativeLayout>
</RelativeLayout>
